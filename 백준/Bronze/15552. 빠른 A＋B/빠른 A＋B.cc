#include <bits/stdc++.h>
using namespace std;

int main()
{
    int T;
    int A, B;

    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> T;
    for (int i = 0; i < T; i++)
    {
        cin >> A >> B;
        cout << A + B << "\n";
    }
    return 0;
}